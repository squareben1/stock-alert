{
  "devDependencies": {
    "jest": "^26.6.3"
  },
  "scripts": {
    "test": "jest --detectOpenHandles",
    "locally": "node -e \"console.log(require('./index').handler([{'symbol': '0P00018XAR.L', 'region': 'en', 'targetMarketChangePercent': -0.01}]));\"",
    "deploy": "./deployScript.sh"
  },
  "dependencies": {
    "aws-sdk": "^2.831.0",
    "axios": "^0.21.1",
    "dotenv": "^8.2.0",
    "jest-aws-sdk-mock": "^1.0.2"
  }
}
